# Output a tile

**Status:** done

## Acceptance criteria

```
Given a user visits index.html
When the page loads
Then it renders a tile on the page
```

## Workflow notes

- Lots of decisions made during this task (build tool, test framework, linting, module structure). Many weren't captured until flagged — workflow now says "capture decisions as they come up."
- Lint step added to workflow.
- "Verify the work" clarified to mean real human confirmation, not just automated tests.
- Workflow restructured into numbered phases (before/during/after) so steps aren't lost in a single paragraph.
